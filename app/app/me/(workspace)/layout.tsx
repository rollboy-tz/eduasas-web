import { motion } from "framer-motion"

const HomeWorkspaceLayout = (
    { children } : { children: React.ReactNode }
) => {
    return(
        <main>

        </main>
    )
}

export default HomeWorkspaceLayout;